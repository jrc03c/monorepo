import {
  ABOVE,
  BELOW,
  EXACTLY_ON,
  getPointRelationToLine,
  pointIsInTriangle,
} from "./point-is-in-triangle.mjs"

test("...", () => {
  throw new Error("No tests have been written for these functions yet!")
})
