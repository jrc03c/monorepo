import { AbortablePromise } from "./index.mjs"

test("tests that the `AbortablePromise` class works as expected", async () => {
  if (AbortablePromise) {
    throw new Error("No tests have been written yet!")
  }
})
