import { BreakablePromise } from "./index.mjs"

test("tests that the `BreakablePromise` class works as expected", async () => {
  if (BreakablePromise) {
    throw new Error("No tests have been written yet!")
  }
})
